#!/bin/bash

MoveFile() {
#       echo "mv" "$originalFile" "$1"
        if [ "$copy_flag" == "1" ]; then
          `cp -f "$originalFile" "$1"`
        else
        `mv "$originalFile" "$1"`
        fi
        `chmod 755 "$1"`
} ## End MoveFile

