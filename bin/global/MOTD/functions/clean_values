#!/bin/bash

function clean_values(){
 [ "$1" == "" ] && minutes=5 || minutes="$1"

for type in "${@:2}"; do
 if [ -f "$type" ]; then
  [ "$(find $type -mmin +$minutes)" != "" ] && rm $type
 fi
done

} ##clean_values
