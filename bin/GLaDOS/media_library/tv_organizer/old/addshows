#!/bin/bash

 while IFS='' read -r line || [[ -n "$line" ]]; do
   #echo "h: $line"
   query="insert into tvShows (id, showname, seasons, isActive) values ('', '$line', '', '')"
   echo "$query" |mysql -D VideoLibrary -s -r -N

 done < "shows.txt"
