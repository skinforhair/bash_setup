#!/bin/bash

LOGGING=0

### Width Variables
widescreen=99
narrowscreen=57

MOUNTS="$DDir/mounts.values"
VIDEOS="$DDir/downloaded_video.values"
DLS="$DDir/active_download.values"

todofile="/shares/incoming/TODO_List"
[ -f $todofile ] && TODO=$(cat "$todofile")
