#!/bin/bash

USERNAME=kodi
PASSWORD=kodi
SERVER=kodi
PORT=80

wget --user=$USERNAME --password=$PASSWORD --delete-after --header='Content-Type: application/json' --post-data='{"jsonrpc": "2.0", "method": "VideoLibrary.Clean"}' http://$SERVER:$PORT/jsonrpc
