#!/bin/bash
for f in /root/.bin/lib/*; do source $f; done

inputFile="$1"
inputFile="${inputFile:0:$((${#inputFile}-4))}"
thumbDir=/shares/glados/wheatley

if [ -d "$thumbDir/$inputFile" ]; then
 echo "Deleting $inputFile thumbnails..."
else
 echo "$thumbDir/$inputFile not found"
fi
