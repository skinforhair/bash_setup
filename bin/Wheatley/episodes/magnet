#!/bin/bash
source /home/jar/.bin/lib/YesNo

usage() {
 echo "Usage: $0 <file.magnet>"
}

if [ "$1" == "" ]; then
	usage
	exit 1
fi

URL=$(cat "$1")

deluge-console add $URL
#YesNo "Remove $1?" "y"
# if [ $? == "1" ]; then
echo "removing $1"
	rm "$1"
# fi
