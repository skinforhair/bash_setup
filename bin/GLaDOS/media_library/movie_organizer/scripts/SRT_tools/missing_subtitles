#!/bin/bash

NUMBERS='^[0-9]+([.][0-9]+)?$'
dbTable="VideoLibrary.movies"

 if [[ $1 =~ $NUMBERS ]]; then
   limit="$1"
 else
   limit=20
 fi

query="select id,year,title from $dbTable where subtitle='none' limit $limit"

echo "$query" |mysql -N
