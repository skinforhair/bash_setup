#!/bin/bash

#Grabs internet IP address
function localWAN(){
 WAN="$(dig +short myip.opendns.com @resolver1.opendns.com)"
 echo -e "$WAN"
} ##localWAN

function Print_localWAN(){
 WAN="$(dig +short myip.opendns.com @resolver1.opendns.com)"
 echo -e "${white}WAN IP: ${cyan}$WAN${NC}"
} ##Print_localWAN

