#!/bin/bash

#function db_quotes_cleaner() {
# for i in title plot tagline actors characters local_path url_path; do


#update movies set plot = replace(plot, "''",'\'');
#update movies set tagline = replace (tagline, "''",'\'');
#title=$(echo "select title from $movieTable where id=$1" | mysql $dbOptions)
# done
#} #END db_quotes_cleaner
